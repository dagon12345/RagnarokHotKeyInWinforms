Updating form online

1st step.
> Change the form or the codes.

2nd step.
>Right click your app the select "Properties" and change the assembly version.

3rd step.
>Change the code version inside must be the same version.text on what you have changed on

4th Step.
>Change the version of the installer inside solution to avoid conflict in updating.
make sure it has the same version of (assembly version, version.text)
-assembly version,
-version.text
-Installer version

5th step
> Build the 2 solution of the project.

6th step
>Upload the file to your GitHub release(remove the latest data then replace it by the new one we've zipped)

7th step
> push the changes